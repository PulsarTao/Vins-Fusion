diff --git a/vins/src/estimator/estimator.cpp b/vins/src/estimator/estimator.cpp
index ac014af..8a95102 100755
--- a/vins/src/estimator/estimator.cpp
+++ b/vins/src/estimator/estimator.cpp
@@ -29,12 +29,26 @@ Estimator::~Estimator()
 void Estimator::clearState()
 {
     mProcess.lock();
+
+    // 本地补丁：清空这三个队列必须持 mBuf 锁。
+    //
+    // 上游这里只持 mProcess，但 accBuf/gyrBuf/featureBuf 是由
+    // inputIMU()/inputImage()/inputFeature() 在【其他线程】持 mBuf 锁写入的。
+    // 两把不同的锁保护同一份数据 = 数据竞争，实测表现为 clearState() 之后
+    // vins_node 段错误(Segmentation fault)。
+    //
+    // 上游没暴露这个问题，是因为 failureDetection() 被 return false 废掉了，
+    // clearState() 在运行时根本不会被调用(只在构造和 changeSensorType 时调，
+    // 那两个场景没有并发写入)。本项目恢复了失效检测与代价失控自愈，
+    // clearState() 变成运行时热路径，这个竞争就必须修。
+    mBuf.lock();
     while(!accBuf.empty())
         accBuf.pop();
     while(!gyrBuf.empty())
         gyrBuf.pop();
     while(!featureBuf.empty())
         featureBuf.pop();
+    mBuf.unlock();
 
     prevTime = -1;
     curTime = 0;
@@ -43,6 +57,22 @@ void Estimator::clearState()
     initR = Eigen::Matrix3d::Identity();
     inputImageCnt = 0;
     initFirstPoseFlag = false;
+    is_static = false;
+    static_count = 0;
+    no_vision_cnt = 0;
+    request_reset = false;
+
+    // 本地补丁：last_P/last_R 也必须清。
+    // 上游 clearState() 只清了 last_marginalization_*，漏了这四个 ——
+    // 在失效检测被禁用时无害，一旦启用就是重置死循环:
+    // 重置后 Ps 归零而 last_P 仍是发散前的旧值，failureDetection() 里的
+    //     (Ps[WINDOW_SIZE] - last_P).norm() > 5.0
+    // 立刻又超阈值，于是不停重置。实测同一个 "单帧位置跳变 9.62 m" 连报 60 次，
+    // 数值一字不差就是这个原因。
+    last_P.setZero();
+    last_R.setIdentity();
+    last_P0.setZero();
+    last_R0.setIdentity();
 
     for (int i = 0; i < WINDOW_SIZE + 1; i++)
     {
@@ -233,6 +263,78 @@ void Estimator::inputFeature(double t, const map<int, vector<pair<int, Eigen::Ma
 }
 
 
+// ---------------------------------------------------------------------------
+// 零速修正(ZUPT)的静止检测 —— 本项目新增
+//
+// 判据用【标准差】而不是原始读数:
+//   * 陀螺静止时读数不为零(它就是零偏，本机实测 0.028 rad/s)，
+//     直接对模长设阈值会把静止误判成运动
+//   * 加速度计静止时读数是重力(约 9.9)，同理不能对模长设阈值
+// 所以看的是「读数波动有多小」。本机实测静止时的标准差:
+//     accel 0.006 m/s²   gyro 0.0006 rad/s
+// 阈值取实测值的约 10 倍，留足余量。
+//
+// 另加一条重力模长校验: 匀加速运动时标准差同样很小，但比力模长会偏离 g，
+// 单看标准差会漏判。
+//
+// 迟滞: 连续 5 个图像区间(约 0.5s)判定为静止才进入静止态，
+// 一旦有一个区间判定为运动就立刻退出 —— 宁可少修正，不可误修正。
+bool Estimator::detectStatic(const vector<pair<double, Eigen::Vector3d>> &accVector,
+                             const vector<pair<double, Eigen::Vector3d>> &gyrVector)
+{
+    const double ACC_STD_TH = 0.06;      // m/s²
+    const double GYR_STD_TH = 0.008;     // rad/s
+    const double G_DEV_TH   = 0.5;       // m/s²，比力模长偏离重力的容许量
+    // 迟滞: 需连续多少个图像区间判定为静止才进入静止态。
+    // 取 10(约 1 秒)而不是 5 —— ZUPT 误判的代价是把运动中的位置冻住，
+    // 远比"少修正一点漂移"严重，所以进入要慢、退出要快(见下面 moving 分支)。
+    const int    HOLD_N     = 10;
+
+    bool moving = true;
+    if (accVector.size() >= 5 && gyrVector.size() == accVector.size())
+    {
+        Eigen::Vector3d am = Eigen::Vector3d::Zero(), gm = Eigen::Vector3d::Zero();
+        for (size_t i = 0; i < accVector.size(); i++)
+        {
+            am += accVector[i].second;
+            gm += gyrVector[i].second;
+        }
+        am /= accVector.size();
+        gm /= gyrVector.size();
+
+        Eigen::Vector3d av = Eigen::Vector3d::Zero(), gv = Eigen::Vector3d::Zero();
+        for (size_t i = 0; i < accVector.size(); i++)
+        {
+            av += (accVector[i].second - am).cwiseAbs2();
+            gv += (gyrVector[i].second - gm).cwiseAbs2();
+        }
+        const double a_std = std::sqrt((av / accVector.size()).maxCoeff());
+        const double g_std = std::sqrt((gv / gyrVector.size()).maxCoeff());
+
+        moving = (a_std > ACC_STD_TH) || (g_std > GYR_STD_TH) ||
+                 (std::fabs(am.norm() - G.norm()) > G_DEV_TH);
+
+    }
+
+    if (moving)
+    {
+        static_count = 0;
+        if (is_static)
+            ROS_INFO("ZUPT: 检测到运动，退出零速修正");
+        is_static = false;
+    }
+    else
+    {
+        static_count++;
+        if (!is_static && static_count >= HOLD_N)
+        {
+            is_static = true;
+            ROS_INFO("ZUPT: 检测到静止，启用零速修正");
+        }
+    }
+    return is_static;
+}
+
 bool Estimator::getIMUInterval(double t0, double t1, vector<pair<double, Eigen::Vector3d>> &accVector, 
                                 vector<pair<double, Eigen::Vector3d>> &gyrVector)
 {
@@ -314,6 +416,8 @@ void Estimator::processMeasurements()
             {
                 // cout << "2-1)" << endl;
                 getIMUInterval(prevTime, curTime, accVector, gyrVector);
+                if (USE_ZUPT)
+                    detectStatic(accVector, gyrVector);   // ZUPT: 更新静止判定
                 // cout << "2-2)" << endl;
             }
 
@@ -369,6 +473,16 @@ void Estimator::processMeasurements()
             // cout << "5-6" << endl;
             mProcess.unlock();
 
+            // 失效重置在这里做 —— 必须在 mProcess 解锁之后，
+            // 因为 clearState()/setParameter() 内部会自己加锁。
+            if (request_reset)
+            {
+                request_reset = false;
+                clearState();
+                setParameter();
+                ROS_WARN("system reboot!");
+            }
+
 
             // cout << "6" << endl;
 
@@ -398,6 +512,33 @@ void Estimator::initFirstIMUPose(vector<pair<double, Eigen::Vector3d>> &accVecto
     }
     averAcc = averAcc / n;
     printf("averge acc %f %f %f\n", averAcc.x(), averAcc.y(), averAcc.z());
+
+    // 本地补丁：重力【模长】也用实测值，不要用配置里的固定 g_norm。
+    //
+    // 上游只用 averAcc 的方向定 R0，模长仍取配置里的 g_norm。但廉价 MEMS
+    // 加速度计有明显的标度误差和热漂: 本机 seeker1 实测静置 |a| 比当地真实
+    // 重力(约 9.79)高 2~3%，而且【一小时内就从 10.069 漂到 9.999】。
+    // 固定的 g_norm 必然对不上，差值成为恒定残差:
+    //     位置误差 ~ 0.5 * (|a| - g_norm) * t²
+    // 实测差 0.07 m/s² 时约 35 米/分钟，正是「静止却单方向持续漂移」的来源。
+    // 零偏本应吸收它，但静止时零偏与重力高度耦合、不总能收敛，实测约 4/5 的
+    // 启动里 Ba 一直停在 0，残差全部变成位置漂移。
+    //
+    // 这里在每次初始化(含失效检测重置后重新初始化)时直接采用当时的真实模长，
+    // 从源头消掉这个恒定残差，零偏只需处理其后的慢漂。
+    // 加合理性检查: 若初始化时平台在运动，averAcc 就不是纯重力，此时退回配置值。
+    {
+        const double g_meas = averAcc.norm();
+        if (g_meas > 9.0 && g_meas < 11.0) {
+            printf("g_norm: 配置 %.4f -> 实测 %.4f (差 %+.4f)\n",
+                   G.z(), g_meas, g_meas - G.z());
+            G.z() = g_meas;
+        } else {
+            printf("g_norm: 实测 %.4f 不合理(初始化时可能在运动)，沿用配置值 %.4f\n",
+                   g_meas, G.z());
+        }
+    }
+
     Matrix3d R0 = Utility::g2R(averAcc);
     double yaw = Utility::R2ypr(R0).x();
     R0 = Utility::ypr2R(Eigen::Vector3d{-yaw, 0, 0}) * R0;
@@ -444,6 +585,9 @@ void Estimator::processIMU(double t, double dt, const Vector3d &linear_accelerat
         Rs[j] *= Utility::deltaQ(un_gyr * dt).toRotationMatrix();
         Vector3d un_acc_1 = Rs[j] * (linear_acceleration - Bas[j]) - g;
         Vector3d un_acc = 0.5 * (un_acc_0 + un_acc_1);
+
+        // 注: 试过静止时跳过这两行(不注入死递推误差)，实测无改善 ——
+        // 优化器本来就会覆盖初值，问题不在初值。
         Ps[j] += dt * Vs[j] + 0.5 * dt * dt * un_acc;
         Vs[j] += dt * un_acc;
     }
@@ -470,6 +614,43 @@ void Estimator::processImage(const map<int, vector<pair<int, Eigen::Matrix<doubl
         //printf("non-keyframe\n");
     }
 
+    // -------------------------------------------------------------------------
+    // 本地补丁：滑窗停滞太久就强制产生关键帧
+    //
+    // 上游只用视差决定关键帧。相机静止时视差 ~= 0，于是每帧都走
+    // MARGIN_SECOND_NEW —— 这个分支只丢弃「次新帧」，窗口里 0..frame_count-2
+    // 这些帧被【冻结】，永远不再更新。后果有两个，第二个才是致命的：
+    //
+    //   1) IMU 预积分无界增长。slideWindow() 把新帧的 IMU 采样并入
+    //      pre_integrations[frame_count-1]（见本文件 slideWindow 内的
+    //      push_back），相机不动就一直并，sum_dt 一路涨，jacobian 超过 1e8
+    //      触发 "numerical unstable in preintegration"。
+    //
+    //   2) 特征饥饿（这才是发散的直接原因）。冻结的那些帧不再接收新观测，
+    //      新特征只能落在最后一个槽位上，攒不够 4 次观测；而优化器只用
+    //      used_num >= 4 的特征(见本文件 optimization() 内的判据)。老特征
+    //      随跟踪丢失逐个消亡后，参与优化的特征数变成 0，视觉约束彻底消失，
+    //      VINS 退化为纯 IMU 递推。实测静置桌面 5 分钟漂了 7.5 公里，
+    //      调试输出里 feat 一栏稳定为 0。
+    //
+    // 只调 keyframe_parallax 治不了本：那等于指望像素噪声或场景里有人走动去
+    // 超过阈值，画面真正静止时依然失效（实测阈值降到 0.3 照样漂公里级）。
+    //
+    // 关键是索引要对：真正无限增长的是 pre_integrations[frame_count - 1]
+    // （合并的目标），而不是 pre_integrations[frame_count]（它每帧都被重建，
+    // 恒等于一个图像周期，检查它等于没检查）。
+    //
+    // 0.5s 的取值：滑窗 10 帧，上限 0.5s 意味着窗口最长覆盖 5 秒，
+    // 零偏引起的位置误差 0.5*b*0.25 是厘米量级，优化器能正常吸收。
+    // 真实运动时每帧视差本就远超阈值，这条分支根本不会触发，对正常工况无影响。
+    // -------------------------------------------------------------------------
+    if (marginalization_flag == MARGIN_SECOND_NEW && USE_IMU && frame_count >= 1 &&
+        pre_integrations[frame_count - 1] != nullptr &&
+        pre_integrations[frame_count - 1]->sum_dt > MAX_KEYFRAME_INTERVAL)
+    {
+        marginalization_flag = MARGIN_OLD;
+    }
+
     ROS_DEBUG("%s", marginalization_flag ? "Non-keyframe" : "Keyframe");
     ROS_DEBUG("Solving %d", frame_count);
     ROS_DEBUG("number of feature: %d", f_manager.getFeatureCount());
@@ -607,9 +788,24 @@ void Estimator::processImage(const map<int, vector<pair<int, Eigen::Matrix<doubl
         {
             ROS_WARN("failure detection!");
             failure_occur = 1;
-            clearState();
-            setParameter();
-            ROS_WARN("system reboot!");
+            // 本地补丁：不能在这里就地 clearState()/setParameter()。
+            //
+            // processImage() 是在 processMeasurements() 持有 mProcess 的情况下
+            // 被调用的(见本文件 processMeasurements 里的 mProcess.lock())，
+            // 而 clearState() 和 setParameter() 内部都会【再次】mProcess.lock()。
+            // mProcess 是不可重入的 std::mutex(estimator.h) -> 自死锁，
+            // 估计器线程永久阻塞: 位姿输出停在触发那一帧、0 Hz，
+            // 而特征跟踪线程还在跑，所以进程看着像活着。
+            //
+            // 实测判定手法: "failure detection!" 打印了但紧随其后的
+            // "system reboot!" 一次都没打印，就是卡在了 clearState() 里。
+            //
+            // 这也是上游把 failureDetection() 用 return false 整个废掉的原因 ——
+            // 恢复路径从来就是死的，一启用就死锁。
+            //
+            // 改为置标志、由 processMeasurements 解锁后执行。
+            // 同一份代码里 changeSensorType() 用的就是这个写法(先 unlock 再调用)。
+            request_reset = true;
             return;
         }
 
@@ -1008,20 +1204,80 @@ void Estimator::double2vector()
 
 bool Estimator::failureDetection()
 {
-    return false;
-    if (f_manager.last_track_num < 2)
+    // 默认沿用上游行为(不检测)。上游把整个函数体废掉是有代价的 ——
+    // 视觉跟踪垮掉时它会静默发散；但实测启用后 clearState() 的恢复路径
+    // 并不可靠，而且阈值稍紧就会在正常运动中误触发、把好的估计重置掉。
+    // 两害相权，默认关闭，需要时在配置里设 use_failure_detection: 1。
+    if (!USE_FAILURE_DETECTION)
+        return false;
+
+    // -------------------------------------------------------------------------
+    // 本地补丁：启用失效检测
+    //
+    // 上游这个函数第一行就是 return false，整个函数体是死代码，而且里面的判据
+    // 大多还被注释成 //return true。也就是说 VINS-Fusion 出厂状态【永远检测不到
+    // 失效】—— 视觉跟踪一旦垮掉(黑暗、遮挡、剧烈运动)，它不会重置，
+    // 而是带着已经发散的状态一路积分下去。
+    //
+    // 本机实测: 房间灯关掉后画面均值从 137 掉到 27.8，特征数从 120/150 崩到
+    // 21/95，VINS 没有任何提示，位置在几分钟内从 6cm 漂到 40m，最终到公里量级。
+    // 对下游(飞控/建图)来说，一个静默发散的位姿比一个明确报错的失效危险得多。
+    //
+    // 这里恢复检测。触发后上层会 clearState() + 重新初始化 —— 会丢掉地图，
+    // 但远好过输出一个几公里外的假位姿。阈值都取得很宽松，只抓真正的失效。
+    // -------------------------------------------------------------------------
+    // 视觉约束真的没了 —— 这是「特征一丢就往一个方向疯狂发散」的直接判据。
+    //
+    // 判据选的是【参与优化的特征数】(used_num >= 4，与 optimization() 里
+    // estimator.cpp:1150 的条件一致)，而不是 last_track_num:
+    //   * last_track_num 是当前帧跟踪到的点数，运动中正常起伏很大，
+    //     拿它做判据会频繁误触发、不断 clearState，比不检测更糟(实测踩过)
+    //   * used_num>=4 的特征才真正进入 ceres 问题。这个数归零意味着
+    //     优化里【一条视觉约束都没有】，位姿完全由 IMU 递推决定 ——
+    //     恒定的零偏误差积分出去就是单方向发散，实测几秒内到几百米。
+    //
+    // 用连续计数做迟滞: 单帧掉零可能只是转身/遮挡的瞬间，能自行恢复；
+    // 连续 15 帧(约 1.5 秒)都为零才判定失效，避免误伤。
     {
-        ROS_INFO(" little feature %d", f_manager.last_track_num);
-        //return true;
+        int usable = 0;
+        for (auto &it : f_manager.feature)
+            if (it.feature_per_frame.size() >= 4) usable++;
+        if (usable < 5) ++no_vision_cnt; else no_vision_cnt = 0;
+        if (no_vision_cnt >= 15)
+        {
+            ROS_WARN("失效检测: 已连续 %d 帧没有可用视觉约束(可优化特征 %d 个) "
+                     "—— 视觉跟丢，正在重置", no_vision_cnt, usable);
+            no_vision_cnt = 0;
+            return true;
+        }
+    }
+    if (Vs[WINDOW_SIZE].norm() > 20.0)
+    {
+        // 手持或小型无人机不可能到 20m/s。到这个量级说明已经在发散，
+        // 早点重置比让它继续积分好。
+        ROS_WARN("失效检测: 速度估计异常(%.1f m/s)", Vs[WINDOW_SIZE].norm());
+        return true;
     }
     if (Bas[WINDOW_SIZE].norm() > 2.5)
     {
-        ROS_INFO(" big IMU acc bias estimation %f", Bas[WINDOW_SIZE].norm());
+        ROS_WARN("失效检测: 加速度计零偏过大(%.3f)", Bas[WINDOW_SIZE].norm());
         return true;
     }
     if (Bgs[WINDOW_SIZE].norm() > 1.0)
     {
-        ROS_INFO(" big IMU gyr bias estimation %f", Bgs[WINDOW_SIZE].norm());
+        ROS_WARN("失效检测: 陀螺零偏过大(%.3f)", Bgs[WINDOW_SIZE].norm());
+        return true;
+    }
+    // 单帧位置跳变。静止长跑时实测到过一次「0.893m -> 325.486m」的单帧突变
+    // (相邻两帧间隔 0.1s，等于 3255 m/s)，是优化器一次灾难性迭代步的结果，
+    // 不是渐进漂移。这类退化无法彻底避免(静止本就是 VIO 的退化构型)，
+    // 但绝不能静默输出 —— 下游飞控拿到这种位姿会直接失控。
+    // 阈值取 5m/帧(相当于 50 m/s)，只抓真正的灾难性突变。
+    // 早前用过 0.5m 的紧阈值，运动时会误触发，反而把好的估计重置掉。
+    if ((Ps[WINDOW_SIZE] - last_P).norm() > 5.0)
+    {
+        ROS_WARN("失效检测: 单帧位置跳变 %.2f m —— 优化器发散",
+                 (Ps[WINDOW_SIZE] - last_P).norm());
         return true;
     }
     /*
@@ -1060,6 +1316,18 @@ void Estimator::optimization()
     TicToc t_whole, t_prepare;
     vector2double();
 
+    // 静止时先把速度清零再冻结 —— 否则冻住的可能是一个已经发散的值。
+    // 清零后 IMU 预积分的速度残差 r_v = delta_v(Ba,Bg) - R_i^T*g*dt
+    // 就唯一确定了加速度计零偏，零偏不再随机游走。
+    if (USE_IMU && is_static)
+    {
+        for (int i = 0; i <= WINDOW_SIZE; i++)
+            para_SpeedBias[i][0] = para_SpeedBias[i][1] = para_SpeedBias[i][2] = 0.0;
+    }
+
+    int _imu_add = 0, _imu_skip = 0; double _max_dt = 0, _min_dt = 1e9;
+    std::vector<ceres::ResidualBlockId> _rb_marg, _rb_imu, _rb_vis;
+
     ceres::Problem problem;
     ceres::LossFunction *loss_function;
     //loss_function = NULL;
@@ -1071,7 +1339,37 @@ void Estimator::optimization()
         ceres::Manifold *local_parameterization = new PoseLocalParameterization();
         problem.AddParameterBlock(para_Pose[i], SIZE_POSE, local_parameterization);
         if(USE_IMU)
-            problem.AddParameterBlock(para_SpeedBias[i], SIZE_SPEEDBIAS);
+        {
+            // 零速修正(ZUPT): 静止时把速度(参数块前 3 维)【硬性冻结】。
+            //
+            // 早前用的是「加一个零速残差」的软约束(权重 1/0.001)，实测只能撑约
+            // 3 分钟: 前 1800 帧速度稳在 0.0002 m/s，之后一路失控到 20 m/s，
+            // 而 ZUPT 判定标志全程为真。原因是软约束要和边缘化先验竞争 ——
+            // 先验会随时间累积信息、变得极其刚硬，最终盖过零速项；
+            // 而且零速项没有进入边缘化，先验里根本不含「速度为零」这个信息。
+            // 改成 SubsetManifold 硬冻结后，优化器根本无法改动速度维度，
+            // 不存在谁压过谁的问题。
+            if (is_static)
+            {
+                // 静止时只冻结【速度】(索引 0..2)。
+                //
+                // 试过连加速度计零偏(索引 3..5)一起冻结 —— 灾难性失败:
+                // 实测 117 帧就发散到 1e84 米。原因是 Ba 是 IMU 残差里必需的
+                // 自由度，冻死它之后残差无论如何都满足不了，优化器直接爆炸。
+                //
+                // 动机本身是对的: 实测静止 5 分钟 Ba 单调涨到 0.4 m/s²，
+                // 对应重力方向偏差 2.3° —— 静止时加速度计零偏与俯仰/横滚
+                // 完全耦合(同一测量既可解释成"有零偏"也可解释成"倾斜了")，
+                // 优化器无从区分，两者一起漂，位置随之线性爬行。
+                // 但正确的处理是给 Ba 加软先验而非硬冻结，这一项尚未实现。
+                static const std::vector<int> kFixedVel{0, 1, 2};
+                problem.AddParameterBlock(para_SpeedBias[i], SIZE_SPEEDBIAS,
+                                          new ceres::SubsetManifold(SIZE_SPEEDBIAS, kFixedVel));
+            }
+            else
+                problem.AddParameterBlock(para_SpeedBias[i], SIZE_SPEEDBIAS);
+        }
+
     }
     if(!USE_IMU)
         problem.SetParameterBlockConstant(para_Pose[0]);
@@ -1100,18 +1398,48 @@ void Estimator::optimization()
     {
         // construct new marginlization_factor
         MarginalizationFactor *marginalization_factor = new MarginalizationFactor(last_marginalization_info);
-        problem.AddResidualBlock(marginalization_factor, NULL,
-                                 last_marginalization_parameter_blocks);
+        _rb_marg.push_back(problem.AddResidualBlock(marginalization_factor, NULL,
+                                 last_marginalization_parameter_blocks));
     }
     if(USE_IMU)
     {
         for (int i = 0; i < frame_count; i++)
         {
             int j = i + 1;
-            if (pre_integrations[j]->sum_dt > 10.0)
+            _max_dt = std::max(_max_dt, pre_integrations[j]->sum_dt);
+            _min_dt = std::min(_min_dt, pre_integrations[j]->sum_dt);
+            // 本地补丁：预积分数值失效时【跳过】该 IMU 因子，而不是照样塞进优化。
+            //
+            // 上游在 imu_factor.h 里已经检测了 jacobian.maxCoeff() > 1e8
+            // ("numerical unstable in preintegration")，但只打印警告，
+            // ROS_BREAK() 被注释掉了，因子照样加入问题。后果实测如下:
+            // 雅可比进入 corrected_delta_p/v/q 后被放大成 ~1e21 的残差，
+            // IMU 因子代价冲到 1e42(同一次求解里视觉代价只有 4.7e4，完全正常)，
+            // Ceres 在这个量级上迭代 1 次就报"收敛"(任何一步的相对改善都低于容差)，
+            // 所有状态原封不动；污染随后被写进边缘化先验，之后每帧都带着它。
+            // 表现就是「间歇性坏初始化」: Ba/Bg 精确冻结、位置纯 IMU 递推漂十几米。
+            //
+            // 用与上游相同的阈值判定，判定为失效就跳过 —— 少一个约束，
+            // 远好于让一个 1e21 的残差毁掉整个优化。
+            const double _jmax = pre_integrations[j]->jacobian.maxCoeff();
+            const double _jmin = pre_integrations[j]->jacobian.minCoeff();
+            if (pre_integrations[j]->sum_dt > 10.0 ||
+                !std::isfinite(_jmax) || !std::isfinite(_jmin) ||
+                _jmax > 1e8 || _jmin < -1e8)
+            {
+                ++_imu_skip;
                 continue;
+            }
+            ++_imu_add;
             IMUFactor* imu_factor = new IMUFactor(pre_integrations[j]);
-            problem.AddResidualBlock(imu_factor, NULL, para_Pose[i], para_SpeedBias[i], para_Pose[j], para_SpeedBias[j]);
+            _rb_imu.push_back(problem.AddResidualBlock(imu_factor, NULL, para_Pose[i], para_SpeedBias[i], para_Pose[j], para_SpeedBias[j]));
+
+            // 零运动约束: 静止时窗口内相邻帧不应产生位移。
+            // 这是【相对】约束，不与边缘化先验争夺绝对位置；配合被冻结的速度，
+            // 让优化器有依据把 processIMU 死递推出来的位置误差拉回去。
+            if (is_static)
+                problem.AddResidualBlock(new ZeroMotionFactor(), NULL,
+                                         para_Pose[i], para_Pose[j]);
         }
     }
 
@@ -1137,7 +1465,7 @@ void Estimator::optimization()
                 Vector3d pts_j = it_per_frame.point;
                 ProjectionTwoFrameOneCamFactor *f_td = new ProjectionTwoFrameOneCamFactor(pts_i, pts_j, it_per_id.feature_per_frame[0].velocity, it_per_frame.velocity,
                                                                  it_per_id.feature_per_frame[0].cur_td, it_per_frame.cur_td);
-                problem.AddResidualBlock(f_td, loss_function, para_Pose[imu_i], para_Pose[imu_j], para_Ex_Pose[0], para_Feature[feature_index], para_Td[0]);
+                _rb_vis.push_back(problem.AddResidualBlock(f_td, loss_function, para_Pose[imu_i], para_Pose[imu_j], para_Ex_Pose[0], para_Feature[feature_index], para_Td[0]));
             }
 
             if(STEREO && it_per_frame.is_stereo)
@@ -1147,18 +1475,37 @@ void Estimator::optimization()
                 {
                     ProjectionTwoFrameTwoCamFactor *f = new ProjectionTwoFrameTwoCamFactor(pts_i, pts_j_right, it_per_id.feature_per_frame[0].velocity, it_per_frame.velocityRight,
                                                                  it_per_id.feature_per_frame[0].cur_td, it_per_frame.cur_td);
-                    problem.AddResidualBlock(f, loss_function, para_Pose[imu_i], para_Pose[imu_j], para_Ex_Pose[0], para_Ex_Pose[1], para_Feature[feature_index], para_Td[0]);
+                    _rb_vis.push_back(problem.AddResidualBlock(f, loss_function, para_Pose[imu_i], para_Pose[imu_j], para_Ex_Pose[0], para_Ex_Pose[1], para_Feature[feature_index], para_Td[0]));
                 }
                 else
                 {
                     ProjectionOneFrameTwoCamFactor *f = new ProjectionOneFrameTwoCamFactor(pts_i, pts_j_right, it_per_id.feature_per_frame[0].velocity, it_per_frame.velocityRight,
                                                                  it_per_id.feature_per_frame[0].cur_td, it_per_frame.cur_td);
-                    problem.AddResidualBlock(f, loss_function, para_Ex_Pose[0], para_Ex_Pose[1], para_Feature[feature_index], para_Td[0]);
+                    _rb_vis.push_back(problem.AddResidualBlock(f, loss_function, para_Ex_Pose[0], para_Ex_Pose[1], para_Feature[feature_index], para_Td[0]));
                 }
                
             }
             f_m_cnt++;
         }
+
+        // 本地补丁：给逆深度加上下界。
+        //
+        // VINS 用逆深度参数化特征(para_Feature = 1/depth)，但上游不对它设界。
+        // 优化过程中逆深度可以被推到趋近 0(深度趋近无穷)甚至为负，
+        // 投影因子里一除就得到 1e20 量级的残差，代价冲到 1e40 以上。
+        // 此时 Ceres 只迭代 1 次就报"收敛"(在这个量级上任何一步的相对改善
+        // 都低于容差)，整个优化停摆 —— 这是「间歇性坏初始化」的另一半原因。
+        // (另一半是三角化没有深度上界，已在 feature_manager.cpp 修复。)
+        //
+        // 区间与三角化的保护一致: 深度 0.1~100 米 -> 逆深度 0.01~10。
+        // 本机双目基线仅 4.6cm，100 米处视差 0.15 像素，深度本就不可信。
+        if (problem.HasParameterBlock(para_Feature[feature_index]))
+        {
+            problem.SetParameterLowerBound(para_Feature[feature_index], 0,
+                                           1.0 / MAX_TRIANGULATE_DEPTH);
+            problem.SetParameterUpperBound(para_Feature[feature_index], 0,
+                                           1.0 / MIN_TRIANGULATE_DEPTH);
+        }
     }
 
     ROS_DEBUG("visual measurement count: %d", f_m_cnt);
@@ -1185,9 +1532,111 @@ void Estimator::optimization()
         options.max_solver_time_in_seconds = SOLVER_TIME * 4.0 / 5.0;
     else
         options.max_solver_time_in_seconds = SOLVER_TIME;
+    {
+        static int _pn = 0;
+        if (++_pn % 25 == 1)
+        {
+            bool sb_const = USE_IMU && problem.HasParameterBlock(para_SpeedBias[0])
+                            && problem.IsParameterBlockConstant(para_SpeedBias[0]);
+            bool pz_const = problem.HasParameterBlock(para_Pose[0])
+                            && problem.IsParameterBlockConstant(para_Pose[0]);
+            printf("  PROBDBG 残差块=%d 参数块=%d | IMU因子 加%d 跳%d sum_dt[%.2f,%.2f] "
+                   "| 视觉=%d fc=%d | SpeedBias常量=%d Pose0常量=%d is_static=%d "
+                   "| Ba=[%.5f %.5f %.5f]\n",
+                   problem.NumResidualBlocks(), problem.NumParameterBlocks(),
+                   _imu_add, _imu_skip, _min_dt, _max_dt, f_m_cnt, frame_count,
+                   sb_const?1:0, pz_const?1:0, is_static?1:0,
+                   para_SpeedBias[WINDOW_SIZE][3], para_SpeedBias[WINDOW_SIZE][4],
+                   para_SpeedBias[WINDOW_SIZE][5]);
+        }
+    }
+    {
+        static int _en = 0;
+        if (++_en % 25 == 1)
+        {
+            auto grp=[&](std::vector<ceres::ResidualBlockId>&v, const char*nm){
+                if (v.empty()) { printf("  %s: (无)", nm); return; }
+                ceres::Problem::EvaluateOptions eo; eo.residual_blocks=v; eo.apply_loss_function=false;
+                double c=0; std::vector<double> r;
+                problem.Evaluate(eo,&c,&r,nullptr,nullptr);
+                double mx=0; for(double x:r) mx=std::max(mx,std::abs(x));
+                printf("  %s: 块%zu 代价%.3e 最大残差%.3e", nm, v.size(), c, mx);
+            };
+            printf("  COSTDBG");
+            grp(_rb_marg,"边缘化"); grp(_rb_imu,"IMU"); grp(_rb_vis,"视觉");
+            printf("\n");
+            for (int i = 0; i < frame_count; i++)
+            {
+                auto *pi = pre_integrations[i+1];
+                if (!pi) continue;
+                printf("    IMU[%d] sum_dt=%.3f |dp|=%.3e |dv|=%.3e jmax=%.3e "
+                       "cov_min=%.3e |Ps|=%.2f |Vs|=%.2f\n",
+                       i+1, pi->sum_dt, pi->delta_p.norm(), pi->delta_v.norm(),
+                       pi->jacobian.cwiseAbs().maxCoeff(),
+                       pi->covariance.diagonal().minCoeff(),
+                       Ps[i+1].norm(), Vs[i+1].norm());
+            }
+        }
+    }
     TicToc t_solver;
     ceres::Solver::Summary summary;
+    double _ba_before[3] = {para_SpeedBias[WINDOW_SIZE][3], para_SpeedBias[WINDOW_SIZE][4],
+                            para_SpeedBias[WINDOW_SIZE][5]};
+    double _v_before[3]  = {para_SpeedBias[WINDOW_SIZE][0], para_SpeedBias[WINDOW_SIZE][1],
+                            para_SpeedBias[WINDOW_SIZE][2]};
     ceres::Solve(options, &problem, &summary);
+    {
+        static int _sn = 0;
+        if (++_sn % 25 == 1)
+            printf("  SOLVEDBG 迭代%2d 代价 %.4e -> %.4e | Ba %.6f->%.6f, %.6f->%.6f, %.6f->%.6f "
+                   "| V %.4f->%.4f | %s\n",
+                   (int)summary.iterations.size(), summary.initial_cost, summary.final_cost,
+                   _ba_before[0], para_SpeedBias[WINDOW_SIZE][3],
+                   _ba_before[1], para_SpeedBias[WINDOW_SIZE][4],
+                   _ba_before[2], para_SpeedBias[WINDOW_SIZE][5],
+                   _v_before[0], para_SpeedBias[WINDOW_SIZE][0],
+                   summary.termination_type == ceres::CONVERGENCE ? "收敛" : summary.message.c_str());
+    }
+
+    // 本地诊断: 优化器一旦不再更新状态，位置就会失控发散。实测多个版本里
+    // Ba 都会在某一刻突然「一字不差地冻住」，随后位置在 2 分钟内从 0.3m
+    // 涨到 2m。这里把 Ceres 自己的终止原因打出来，才能知道它到底是
+    // 收敛了、还是因为代价非有限/线性求解失败之类的原因直接放弃了。
+    // 本地补丁：代价数值失控就自愈 —— 请求重新初始化。
+    //
+    // 实测「间歇性坏初始化」的判据极其干净(回放同一份数据，仅时序不同):
+    //     健康: 首次求解 代价 1.0e+07 -> 1.3e+03，之后稳定收敛
+    //     失效: 首次求解 代价 6.2e+42 -> 6.2e+42，一步不降
+    // 两者差 35 个数量级，不存在误判空间。
+    // 失效的成因是初始化期间的时序竞态导致某个 IMU 因子数值失效
+    // (实测该因子最大残差 3.5e21，而同一次求解里视觉残差只有 1.1e2)；
+    // Ceres 在这个量级上迭代 1 次就报"收敛"，所有状态原封不动，
+    // 污染随后写进边缘化先验，之后每帧都带着它 —— 位置纯 IMU 递推漂十几米。
+    //
+    // 这种状态【无法自行恢复】，只能重新初始化。阈值 1e12 远高于正常量级(1e7)、
+    // 又远低于失效量级(1e42)，取中间任意值都可以。
+    // 重置走的是 request_reset 标志(在 processMeasurements 解锁后执行)，
+    // 不能就地 clearState()，否则自死锁 —— 见本文件失效检测处的说明。
+    if (solver_flag == NON_LINEAR && std::isfinite(summary.final_cost) &&
+        summary.final_cost > 1e12)
+    {
+        ROS_WARN("优化代价数值失控 (%.3e)，初始化已被污染，请求重新初始化",
+                 summary.final_cost);
+        request_reset = true;
+    }
+    else if (!std::isfinite(summary.final_cost))
+    {
+        ROS_WARN("优化代价非有限，请求重新初始化");
+        request_reset = true;
+    }
+
+    if (summary.termination_type != ceres::CONVERGENCE)
+    {
+        ROS_WARN("Ceres 未收敛: %s | 迭代 %d | 代价 %.6e -> %.6e",
+                 summary.message.c_str(),
+                 static_cast<int>(summary.iterations.size()),
+                 summary.initial_cost, summary.final_cost);
+    }
     //cout << summary.BriefReport() << endl;
     ROS_DEBUG("Iterations : %d", static_cast<int>(summary.iterations.size()));
     //printf("solver costs: %f \n", t_solver.toc());
diff --git a/vins/src/estimator/estimator.h b/vins/src/estimator/estimator.h
index bf27b8c..c4d0b99 100755
--- a/vins/src/estimator/estimator.h
+++ b/vins/src/estimator/estimator.h
@@ -34,6 +34,7 @@
 #include "../factor/projectionTwoFrameOneCamFactor.h"
 #include "../factor/projectionTwoFrameTwoCamFactor.h"
 #include "../factor/projectionOneFrameTwoCamFactor.h"
+#include "../factor/zero_motion_factor.h"
 #include "../featureTracker/feature_tracker.h"
 
 #define ROS_INFO RCUTILS_LOG_INFO
@@ -177,5 +178,18 @@ class Estimator
     Eigen::Quaterniond latest_Q;
 
     bool initFirstPoseFlag;
+
+    // ---- 零速修正(ZUPT)，本项目新增 ----
+    // 静止时加速度计零偏不可观会一路游走，最终把位置带飞。
+    // 详见 factor/zero_velocity_factor.h 的说明与实测数据。
+    bool detectStatic(const std::vector<std::pair<double, Eigen::Vector3d>> &accVector,
+                      const std::vector<std::pair<double, Eigen::Vector3d>> &gyrVector);
+    bool is_static;          // 当前是否判定为静止
+    int  static_count;       // 连续判定为静止的区间数(用于迟滞)
+    int  no_vision_cnt;      // 连续多少帧没有可用视觉约束(失效检测用)
+    // 失效检测请求重置。实际的 clearState()/setParameter() 必须放到
+    // processMeasurements 解锁之后执行，不能在 processImage 里就地调用 ——
+    // 那样会自死锁，详见 estimator.cpp 里的说明。
+    bool request_reset;
     bool initThreadFlag;
 };
diff --git a/vins/src/estimator/feature_manager.cpp b/vins/src/estimator/feature_manager.cpp
index d6022a4..1b3c277 100755
--- a/vins/src/estimator/feature_manager.cpp
+++ b/vins/src/estimator/feature_manager.cpp
@@ -334,7 +334,21 @@ void FeatureManager::triangulate(int frameCnt, Vector3d Ps[], Matrix3d Rs[], Vec
             Eigen::Vector3d localPoint;
             localPoint = leftPose.leftCols<3>() * point3d + leftPose.rightCols<1>();
             double depth = localPoint.z();
-            if (depth > 0)
+            // 本地补丁：深度必须有【上界】。
+            //
+            // 上游只判 depth > 0。但时间三角化用的是相邻两帧(imu_i 与 imu_i+1)，
+            // 相机静止时这两帧位姿几乎相同 -> 基线为零 -> 三角化退化 ->
+            // 深度可以是 1e10 甚至 1e20 的任意值。
+            // VINS 用逆深度参数化(para_Feature = 1/depth)，逆深度 ~1e-20 时
+            // 投影因子的残差会到 1e20 量级，代价冲到 1e40 以上。
+            // 此时 Ceres 只迭代 1 次就报"收敛"(在这个量级上任何一步的相对改善
+            // 都低于容差)，所有状态原封不动 —— 表现为 Ba/Bg 精确冻结、
+            // 位置纯 IMU 递推发散十几米。这就是一直没定位的「间歇性坏初始化」。
+            //
+            // 上界取 MAX_TRIANGULATE_DEPTH: 本机双目基线仅 4.6cm，fx=320，
+            // 100 米处视差只有 0.15 像素，远低于噪声，深度本就不可信。
+            // 超界的退回 INIT_DEPTH，与上游处理 depth<=0 的方式一致。
+            if (depth > MIN_TRIANGULATE_DEPTH && depth < MAX_TRIANGULATE_DEPTH)
                 it_per_id.estimated_depth = depth;
             else
                 it_per_id.estimated_depth = INIT_DEPTH;
@@ -369,7 +383,8 @@ void FeatureManager::triangulate(int frameCnt, Vector3d Ps[], Matrix3d Rs[], Vec
             Eigen::Vector3d localPoint;
             localPoint = leftPose.leftCols<3>() * point3d + leftPose.rightCols<1>();
             double depth = localPoint.z();
-            if (depth > 0)
+            // 同上：深度必须有上界，防止退化三角化产生天文数字深度。
+            if (depth > MIN_TRIANGULATE_DEPTH && depth < MAX_TRIANGULATE_DEPTH)
                 it_per_id.estimated_depth = depth;
             else
                 it_per_id.estimated_depth = INIT_DEPTH;
diff --git a/vins/src/estimator/parameters.cpp b/vins/src/estimator/parameters.cpp
index 71afb67..df061f0 100755
--- a/vins/src/estimator/parameters.cpp
+++ b/vins/src/estimator/parameters.cpp
@@ -28,6 +28,8 @@ double BIAS_GYR_THRESHOLD;
 double SOLVER_TIME;
 int NUM_ITERATIONS;
 int ESTIMATE_EXTRINSIC;
+int USE_ZUPT;
+int USE_FAILURE_DETECTION;
 int ESTIMATE_TD;
 int ROLLING_SHUTTER;
 std::string EX_CALIB_RESULT_PATH;
@@ -116,6 +118,10 @@ void readParameters(std::string config_file)
 
     SOLVER_TIME = fsSettings["max_solver_time"];
     NUM_ITERATIONS = fsSettings["max_num_iterations"];
+    USE_ZUPT = fsSettings["use_zupt"].empty() ? 0 : (int)fsSettings["use_zupt"];
+    printf("use_zupt %d\n", USE_ZUPT);
+    USE_FAILURE_DETECTION = fsSettings["use_failure_detection"].empty() ? 0 : (int)fsSettings["use_failure_detection"];
+    printf("use_failure_detection %d\n", USE_FAILURE_DETECTION);
     MIN_PARALLAX = fsSettings["keyframe_parallax"];
     MIN_PARALLAX = MIN_PARALLAX / FOCAL_LENGTH;
 
diff --git a/vins/src/factor/zero_motion_factor.h b/vins/src/factor/zero_motion_factor.h
new file mode 100644
index 0000000..f6e2921
--- /dev/null
+++ b/vins/src/factor/zero_motion_factor.h
@@ -0,0 +1,70 @@
+/*******************************************************
+ * 零运动约束 —— 本项目新增，非上游文件
+ *
+ * 静止时约束窗口内相邻两帧的位移为零: residual = P_j - P_i
+ *
+ * 为什么不能用「冻结位姿」代替(实测走过的弯路):
+ *   processIMU() 在优化【之前】就用 IMU 死递推推进了新帧的位姿:
+ *       Ps[j] += dt * Vs[j] + 0.5 * dt * dt * un_acc;
+ *   这是给优化器的初值。如果这时把位姿 SetParameterBlockConstant，
+ *   等于把这个带着零偏误差的递推结果【锁死】，优化器再也没机会纠正它。
+ *   实测: 速度精确为 0、零偏也正常，位置照样 4 分钟漂 2.23 米。
+ *   冻结不是「锁住不动」，是「锁住错误」。
+ *
+ * 为什么也不能只靠零速约束:
+ *   IMU 位置残差里的 delta_p 由加速度计零偏决定，零偏一错，
+ *   每个窗口步长都会给位置加一个固定增量。而零偏在静止时不可观，
+ *   还会被边缘化先验锁死在错误值上(实测反复冻结在 0.68 / 0.21)。
+ *   实测只冻速度: 位置仍从 1.2cm 漂到 1.22m(8 分钟)。
+ *
+ * 这里用【相对】约束而不是绝对约束，有两个好处:
+ *   1) 不与边缘化先验争夺绝对位置，避免早前软约束被先验压过的问题
+ *   2) 优化器仍可自由调整整体位姿去满足视觉观测，只是不许窗口内部产生位移
+ *******************************************************/
+
+#pragma once
+
+#include <ceres/ceres.h>
+#include <eigen3/Eigen/Dense>
+
+// 参数块是两个 para_Pose，布局为 [P(0..2), Q(3..6)]，这里只约束平移部分。
+class ZeroMotionFactor : public ceres::SizedCostFunction<3, 7, 7>
+{
+  public:
+    // sigma 取 2mm。实测收紧到 0.2mm 反而略差(1.37cm vs 1.17cm)，
+    // 说明约束强度不是残余漂移的瓶颈。
+    // 早前用 2mm，实测 ZUPT 全程激活、速度精确为 0 的情况下位置仍以约
+    // 0.2~0.5 cm/分钟 缓慢爬行 —— 因为这是【相邻帧】的相对约束，
+    // 单帧残差很小但滑窗反复滑动后会累积。收紧 10 倍以压住累积项。
+    // 仍是软约束，视觉观测依然能让整体位姿平滑调整，不会像硬冻结那样
+    // 把 processIMU 死递推出来的误差锁死(那条路实测更差，见文件头说明)。
+    static constexpr double SIGMA = 0.002;
+
+    virtual bool Evaluate(double const *const *parameters, double *residuals,
+                          double **jacobians) const
+    {
+        const double w = 1.0 / SIGMA;
+        for (int k = 0; k < 3; ++k)
+            residuals[k] = w * (parameters[1][k] - parameters[0][k]);
+
+        // 注意 jacobian 的列数是 7(参数块的 ambient size)，
+        // 但 PoseLocalParameterization 的切空间是 6 维，
+        // ceres 会用 manifold 的 PlusJacobian 自动做投影，这里按 ambient 写即可。
+        if (jacobians)
+        {
+            if (jacobians[0])
+            {
+                Eigen::Map<Eigen::Matrix<double, 3, 7, Eigen::RowMajor>> J(jacobians[0]);
+                J.setZero();
+                J.block<3, 3>(0, 0) = -w * Eigen::Matrix3d::Identity();
+            }
+            if (jacobians[1])
+            {
+                Eigen::Map<Eigen::Matrix<double, 3, 7, Eigen::RowMajor>> J(jacobians[1]);
+                J.setZero();
+                J.block<3, 3>(0, 0) = w * Eigen::Matrix3d::Identity();
+            }
+        }
+        return true;
+    }
+};
diff --git a/vins/src/featureTracker/feature_tracker.cpp b/vins/src/featureTracker/feature_tracker.cpp
index 3508bc0..ff1a657 100755
--- a/vins/src/featureTracker/feature_tracker.cpp
+++ b/vins/src/featureTracker/feature_tracker.cpp
@@ -356,8 +356,32 @@ map<int, vector<pair<int, Eigen::Matrix<double, 7, 1>>>> FeatureTracker::trackIm
             {
                 TicToc t_check;
                 vector<float> err;
+
+                // 本地补丁：给双目 LK 一个视差初值，而不是从零视差盲搜。
+                //
+                // 上游把 cur_right_pts 传空，等价于假设初始视差为 0，让 LK 自己搜。
+                // 但本项目的输入是【已矫正】双目(实测 dy 中位数 0.33 像素)，
+                // 视差几乎纯水平，且同一特征上一帧的视差就是极好的初值。
+                // 近处物体视差可达上百像素，远超 LK 的有效搜索范围
+                // (21x21 窗口 x 3 层金字塔)，零初值必然搜不到 —— 实测某场景里
+                // 90 个特征只有 30 个匹配成功(33%)，47 个死在反向校验。
+                // 而双目匹配数偏低又与「坏初始化」强相关(实测好的 44~58 个，
+                // 坏的只有 24~36 个)，因为深度约束不足会让尺度和零偏都定不下来。
+                //
+                // 这里用上一帧的同 id 视差做初值，配合 OPTFLOW_USE_INITIAL_FLOW。
+                // 没有历史的新特征退回零视差(与上游一致)，不会变差。
+                cur_right_pts.resize(cur_pts.size());
+                for (size_t i = 0; i < cur_pts.size(); i++)
+                {
+                    auto it = prev_disparity.find(ids[i]);
+                    const float d = (it != prev_disparity.end()) ? it->second : 0.0f;
+                    cur_right_pts[i] = cv::Point2f(cur_pts[i].x - d, cur_pts[i].y);
+                }
                 // cur left ---- cur right
-                cv::calcOpticalFlowPyrLK(cur_img, rightImg, cur_pts, cur_right_pts, status, err, cv::Size(21, 21), 3);
+                cv::calcOpticalFlowPyrLK(cur_img, rightImg, cur_pts, cur_right_pts, status, err,
+                                         cv::Size(21, 21), 3,
+                                         cv::TermCriteria(cv::TermCriteria::COUNT + cv::TermCriteria::EPS, 30, 0.01),
+                                         cv::OPTFLOW_USE_INITIAL_FLOW);
                 // reverse check cur right ---- cur left
                 if(FLOW_BACK)
                 {
@@ -419,6 +443,16 @@ map<int, vector<pair<int, Eigen::Matrix<double, 7, 1>>>> FeatureTracker::trackIm
                 // printf("gpu left right optical flow cost %fms\n",t_og1.toc());
             }
 #endif
+            // 本地补丁：把本帧匹配成功的视差存下来，作为下一帧 LK 的初值。
+            // 只存匹配成功的，失败的从缓存里删掉，避免用一个错误的视差反复误导。
+            for (size_t i = 0; i < status.size() && i < ids.size(); i++)
+            {
+                if (status[i])
+                    prev_disparity[ids[i]] = cur_pts[i].x - cur_right_pts[i].x;
+                else
+                    prev_disparity.erase(ids[i]);
+            }
+
             ids_right = ids;
             reduceVector(cur_right_pts, status);
             reduceVector(ids_right, status);
@@ -534,6 +568,26 @@ void FeatureTracker::rejectWithF()
 
 void FeatureTracker::readIntrinsicParameter(const vector<string> &calib_file)
 {
+    // 本地补丁：重复调用时直接返回。
+    //
+    // 上游这里无条件 push_back，从不 clear。正常启动只调用一次，
+    // 所以看不出问题；但本项目恢复了失效检测/代价失控自愈，重置时会再次
+    // 调用 setParameter()，而 setParameter() 里就有这一行。后果有两层:
+    //
+    //   1) m_camera 每重置一次就多 2 个相机 (2 -> 4 -> 6)，与 NUM_OF_CAM 脱节
+    //   2) 【致命】push_back 会让 vector 重新分配缓冲区，而此时
+    //      特征跟踪线程正在并发使用 m_camera[i] —— 读到的是已释放内存里的
+    //      shared_ptr，解引用即野指针 -> 段错误。
+    //
+    // 实测: 每次触发重置后随机崩溃(dmesg 全是 "segfault at 680 error 4")，
+    // 而在 gdb 下因为整体减速反而不复现 —— 典型的竞态特征。
+    //
+    // 这里不能改成 "clear() 再 push_back"，那样窗口期 m_camera 是空的，
+    // 跟踪线程的 m_camera[0] 会直接越界，比原来更糟。内参本来就不会变，
+    // 已加载过就整个跳过才是对的。
+    if (m_camera.size() == calib_file.size())
+        return;
+
     for (size_t i = 0; i < calib_file.size(); i++)
     {
         ROS_INFO("reading paramerter of camera %s", calib_file[i].c_str());
@@ -605,7 +659,17 @@ vector<cv::Point2f> FeatureTracker::ptsVelocity(vector<int> &ids, vector<cv::Poi
     }
 
     // caculate points velocity
-    if (!prev_id_pts.empty())
+    //
+    // 本地补丁：dt <= 0 时不能做除法。
+    // 相机偶发给出重复时间戳(本机 seeker1 实测约 1%: 400 帧里 4~5 次)，
+    // 此时 dt = 0，下面的除法得到 Inf -> 投影因子算出 NaN ->
+    // Ceres 报 "Residual and Jacobian evaluation failed"，迭代 0 次直接放弃。
+    // NaN 一旦污染状态或边缘化先验，之后【每一帧】都失败(实测 133/132 帧全失败)，
+    // 优化器彻底停摆、所有状态冻结，位置只能靠 IMU 递推单方向发散。
+    // 表现为「间歇性坏初始化」——初始化那几秒撞上一次重复，整次运行就废了。
+    // 这里退化为速度全零(与没有上一帧时的处理一致)，代价只是这一帧不提供
+    // 时间偏移的观测，远好于让 NaN 毁掉整个估计器。
+    if (!prev_id_pts.empty() && cur_time - prev_time > 1e-9)
     {
         double dt = cur_time - prev_time;
         
diff --git a/vins/src/featureTracker/feature_tracker.h b/vins/src/featureTracker/feature_tracker.h
index 7f4d8f5..0ec42dc 100755
--- a/vins/src/featureTracker/feature_tracker.h
+++ b/vins/src/featureTracker/feature_tracker.h
@@ -90,6 +90,9 @@ public:
     vector<int> track_cnt;
     map<int, cv::Point2f> cur_un_pts_map, prev_un_pts_map;
     map<int, cv::Point2f> cur_un_right_pts_map, prev_un_right_pts_map;
+    // 本地补丁：每个特征上一帧的双目视差(左目x - 右目x)，用作下一帧 LK 的初值。
+    // 见 feature_tracker.cpp 里双目匹配处的说明。
+    map<int, float> prev_disparity;
     map<int, cv::Point2f> prevLeftPtsMap;
     vector<camodocal::CameraPtr> m_camera;
     double cur_time;
